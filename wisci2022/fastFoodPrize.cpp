#include <iostream>
#include <vector>

using namespace std;

int main(int argc, char const *argv[])
{
    int n;
    cin >> n;
    for (int i = 0; i < n; i++)
    {
        int n, m;
        cin >> n >> m;
        vector<vector<int>> tt; 
        vector<int> ct;
        for (int j = 0; j < n; j++)
        {
            int e; 
            cin >> o;
            tt.push_back(new vector<int>);
            for (int k = 0; k <= o; k++)
            {
                cin >> e;
                tt[j].push_back(e);
            }
        }
        for (int j = 0; j < m; j++)
        {
            int e;
            cin >> e;
            ct.push_back(e);
        }
    }
    
    return 0;
}
